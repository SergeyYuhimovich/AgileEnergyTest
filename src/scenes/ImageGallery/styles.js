import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  imageGallery: {
    flex: 1,
    justifyContent: 'center',
    paddingHorizontal: 5,
    backgroundColor: '#000'
  }
})
